import { CollectionsService } from '../../collections.service';
import { CollectionExistPipePipe } from './collection-exist-pipe.pipe';

describe('CollectionExistPipePipe', () => {
  it('should be defined', () => {
    expect(new CollectionExistPipePipe()).toBeDefined();
  });
});
