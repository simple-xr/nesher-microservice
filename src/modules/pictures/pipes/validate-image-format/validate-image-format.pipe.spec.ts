import { ValidateImageFormatPipe } from './validate-image-format.pipe';

describe('ValidateImageFormatPipe', () => {
  it('should be defined', () => {
    expect(new ValidateImageFormatPipe()).toBeDefined();
  });
});
